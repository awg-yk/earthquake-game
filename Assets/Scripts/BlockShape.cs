namespace EarthquakeGame
{
    // Only rectangles - see BlockTowerManager.GetBlockSize(), which always
    // returns the same fixed elongated rectangle. The player only controls
    // its rotation and drop position.
    public enum BlockShape
    {
        Rectangle
    }
}
